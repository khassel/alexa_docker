version: '3'

# for build only
services:
  alexa:
    build: 
      context: ./
      args:
        myos: "rpi"
        myimage: "resin/rpi-raspbian:jessie"
    container_name: alexa
    image: karsten13/alexa:rpi