version: '3'

# for build only
services:
  alexa:
    build: 
      context: ./
      args:
        myos: "rpi"
        myimage: "arm32v7/debian:jessie-backports"
    container_name: alexa
    image: karsten13/alexa:rpi