version: '3'

# for build only
services:
  alexa:
    build: 
      context: ./
      args:
        myos: "rpi"
        myimage: "arm32v7/node:latest"
    container_name: alexa
    image: karsten13/alexa:rpi
