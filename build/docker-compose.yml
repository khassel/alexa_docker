version: '3'

# for build only
services:
  alexa:
    build: 
      context: ./
      args:
        myos: "rpi"
        myimage: "karsten13/alexa-base:rpi"
    container_name: alexa
    image: karsten13/alexa:rpi