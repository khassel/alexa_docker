version: '3'

# for build only
services:
  alexa:
    build: 
      context: ./
      args:
        myos: "ubuntu64"
        myimage: "node:latest"
    container_name: alexa
    image: karsten13/alexa:ubuntu64